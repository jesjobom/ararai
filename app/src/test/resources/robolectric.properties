# Robolectric's Android 36 sandbox requires Java 21. The project toolchain remains
# on JDK 17, so JVM Android tests use the latest compatible SDK unless a test
# explicitly selects another supported SDK with @Config.
sdk=35
